import type { BreedConfig } from "../types";
import { breedSpriteSets } from "../types";
import src from "./jess.png";

export const jess: BreedConfig = { src, spriteSets: breedSpriteSets, gap: 1, cols: 8, rows: 4 };
