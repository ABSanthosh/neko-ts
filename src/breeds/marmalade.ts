import type { BreedConfig } from "../types";
import { breedSpriteSets } from "../types";
import src from "./marmalade.png";

export const marmalade: BreedConfig = { src, spriteSets: breedSpriteSets, gap: 1, cols: 8, rows: 4 };
